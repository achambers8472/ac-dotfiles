#!/usr/bin/env bash

set -o errexit
set -o pipefail
set -o nounset

tmp_output="$(mktemp XXXXXXXXXXXXXXXX.tmp)"
trap "rm '${tmp_output}'" EXIT
python -m cProfile -o "${tmp_output}" $@
runsnake "${tmp_output}"
